#!/usr/bin/env node
const node = process.argv[0];
const path = require('path').join(__dirname, '_index');
const args = [].concat(path).concat(process.argv.slice(2));
/**
 * Spawn a new Node process
 */
require('child_process').spawn(node, args, {stdio: 'inherit'});
