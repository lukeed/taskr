'use strict';

module.exports = function () {
	this.plugin('plugTwo', {every: 0}, function * (arr, o) {
		for (let file of arr) {
			file.data = new Buffer(file.data.toString().split('').reverse().join(''));
		}
	});
};
